machine:
  node:
    version:
      6.3.0

dependencies:
    pre:
      - npm install -g karma
    cache_directories:
      - ~/nvm

test:
    override:
      - npm install
      - nohup bash -c "karma start &"