machine:
  node:
    version:
      6.3.0

dependencies:
    pre:
      - npm install -g karma
    cache_directories:
      - ~/nvm

test:
    pre:
      - nohup bash -c "karma start &"
    override:
      - npm install
      - karma run